# Maintainer
Servidor utilizado para gerir os servidores do gled, usando autenticação única por cada servidor que é depois armazenada na base de dados.
Foi usado o go para fazer a API que ficou separada do frontend
É também utilizado um serviço vpn para conectar os servidores de forma segura.